// Online C++ compiler to run C++ program online
#include <bits/stdc++.h>
using namespace std;

int main() {
    //vector<int> p;
    //p.push_back(2);
    //p.emplace_back(9);
    //cout<<p[1];
    //vector<int> v;
    //v.push_back(8);
    //v.emplace_back(9);
    //v.push_back({1,2});
    //v.emplace_back(2,3);

    //cout <<v[1].second;
    //vector<int> v(5,100); "it stores 100 5 times in v"
    vector<int> v={0,1,2,4,5,6,7,8};
    v.push_back(9);
    v.emplace_back(10);
    v.erase(v.begin(),v.begin()+1); //v.erase(start,end)
    v.insert(v.begin()+2,1,3);//it inserts "3" 1 time
    v.pop_back(); //removes last element
    
    
    //vector<int>::iterator it=v.end();
    //it--;
    //while(*(it)!=0){
        //cout<<*(it)<<",";
      //  it--;
    //}
    //cout<<endl<<v.back();
    /*vector<int>::iterator it=v.end();
    it--;
    for(it;it!= v.begin();it--){
        cout<<*(it)<<",";
    }
    cout<<*(it++); 
    */
    //instaed of vector<int>::iterator=v.begin() use "auto"
    vector<int>p(2,100);
    p.emplace_back(200);
    v.insert(v.begin(),p.begin(),p.end());
    
    
    for(auto it=v.begin();it!=v.end();it++){
        cout<<*(it)<<",";
    }
    cout<<endl;
    //EASY ITERATION METHOD TO ACCSESS ELEMENTS
    for(auto it:v){
        cout<<it<<" ";
    }
    cout<<endl<<v.size();
    
    //v1={1,2} v2={3,4}
    //v1.swap(v2);
    
    //v.clear(); //erases entire vector
    
    return 0;

    
}
